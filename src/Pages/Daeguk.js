import Header from "../Components/Header";

const Daeguk = ({ members }) => {
  return (
    <div>
      <Header members={members} />
      <h1>대국의 페이지</h1>
    </div>
  );
};

export default Daeguk;
